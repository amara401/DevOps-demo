https://raymii.org/s/tutorials/Shared_Git_repository_via_ssh_for_multiple_users.html
https://www.edureka.co/blog/install-git/

-----Git Installation-----
#sudo yum groupinstall "Development Tools"
#sudo yum install gettext-devel openssl-devel perl-CPAN perl-devel zlib-devel
#wget https://github.com/git/git/archive/v2.7.2.tar.gz -O git.tar.gz
#tar -xvf git.tar.gz
#cd git-2.7.2
#make configure
#./configure --prefix=/usr/local
#sudo make install

-----GIT Configuration-----
#git --version
#git config --global user.name "amara401"
#git config --global user.email "mada.amarnadh@gmail.com"
#git config --list
#ssh-keygen -t rsa -b 4096 -C "your_email@example.com"
#eval "$(ssh-agent -s)"		---> ensure that SSH agent is enabled
#ssh-add ~/.ssh/id_rsa		---> To add SSH key to the SSH agent we will use
#cat ~/.ssh/id_rsa.pub		---> To add SSH key to our GitHub account we will use
#Finally we need to copy the SSH key and then we need to go to the GitHub account and click on settings.
#and then go to SSH and GPG keys option on the left.
#now click on New SSH key and add title to it  and then paste the copied key in the space provided. Now we will click on add SSH key
#ssh -T git@github.com		---> Now use the below command to test the SSH key:


-----GIT_Commands-----
#git reset HEAD~1 			---> revert the committed files into unstaged files




 